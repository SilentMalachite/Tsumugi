# Open Questions（仕様未確定・要確認）

> 推測で実装しない。確定するまで該当機能の実装は止める。

- [x] **[Phase3-1/AC3-3] `r8-grant-decision-administration-202606` のSHA・物理頁不整合（2026-07-11クローズ / ADR 0020・0025）**: ADR 0020登録済みの北九州市公式サイト再配布PDFを正本として再取得し、SHA-256 `c5070de88b83528860e8dba6c4aa88ec4bd7418dea017fbbdb5cc80dc7014798`、1,968,795 bytes、262 pagesを確認した。物理頁182〜186・197〜199は令和8年7月版の同頁と抽出本文が完全一致し、197〜199を目視でも照合した。ADR 0025の旧SHA・旧URLをADR 0020登録値へ統一し、ADR 0020の利用locatorへ197〜199を追記したため、AC3-3の停止条件を解除する。
- [x] **報酬の単位数・加算・地域区分単価の公式出典（2026-07-11 クローズ / ADR 0020）**: 令和6年4月・6月と令和8年6月の一次資料、適用版、取得日、SHA-256、訂正関係を [ADR 0020](decisions/0020-claim-master-sources-and-versioning.md) で確定し、`JsonClaimMasterProviderTests` で出典カタログと版境界を固定した。Phase 3-0で確定したのは出典・版・適用規則であり、制度実値をC#へ直書きしていない。その後、基準該当B型の公式計算契約とsource inventoryを実装したが、production seedへの実値投入とruntime請求計算は引き続きPhase 3-1の未完了範囲である。
- [x] **[Phase3-1/Task13] 基準該当B型の公式計算契約・source inventory schema-gap（2026-07-15 クローズ / ADR 0020・0025）**: 公式式、比較対象、地方公共団体0.965補正、保護施設事務費runtime入力要件及びprovenanceをDomain・JSON Schema・validatorのclosed contractで固定した。公式3資料とrelease bundleをsource catalogへ追加し、inventoryを44 documents / 61 ranges / 14,726 rows、seed 14,189 / excluded 537 / schema-gap 0、ordered identity SHA-256 `c80f4e8da0aefc9d91bd978777bdb8e59261f4982826555f8a324e2023b9bcd7`へ確定した。R6への現行統合告示利用はdirect evidenceではなくcontinuity inferenceとして記録し、H31資料はcross-checkに限定した。このクローズはschema-gap解消であり、runtime算定完了を意味しない。
- [ ] **[Phase3-1/Task13 follow-up] 保護施設事務費実値・production seed・runtime算定**: `protected-facility-administrative-expense-yen`は施設・サービス・年度単位で後入力する必須runtime inputとして契約化済みだが、実値record、対象service-code revisionsのproduction seed、master resolver及びruntime calculatorは未実装である。欠損値を0円又は最新値として推測せず、施設・地域・定員・加算・決定主体・通知・対象年度4月1日時点のprovenanceを保持して入力・解決・算定を実装し、別途受け入れる。
- [ ] **[Phase3-1/Task7] 受給者証の月内切替時の請求取扱い**: 月途中の受給者証切替（有効期間が月内で分かれる場合）の請求分割・適用証の公式取扱い — Phase 3-1では複数証検出時にreadinessで算定不能とし、確定できるまで対象外。
- [ ] **[Phase3-1/Task11] 送迎加算の「別に厚生労働大臣が定める送迎」要件本文（2026-07-19 追加 / ADR 0028）**: 送迎体制(Ⅰ)(Ⅱ)の適格要件と同一敷地内70%の適用条件を定める告示が `sources.json` に未登録で、要件本文を検証できない。算定は `transport-system` の届出値と片道実績から行い、届出要件の充足検証は要件告示を登録・確定するまでスコープ外とする。
- [ ] **[Phase3-1/Task11] R8-06の福祉・介護職員等処遇改善加算の率・新コード対応（2026-07-19 追加 / ADR 0028）**: `current-fee-notice-html` で(Ⅰ)イ105・(Ⅰ)ロ109・(Ⅱ)イ103・(Ⅱ)ロ107・(Ⅲ)88・(Ⅳ)74（各1000分の）を観測したが、R8新コード構成（465174〜465176追加）との対応と `r8-fee-notice` からの正式抽出は未実施。`claim-master-r8-06` の処遇改善seedは別ADRで確定するまで投入しない。
- [ ] **[Phase3-1/Task13] 平均工賃月額からの区分（band）自動導出（2026-07-19 追加 / ADR 0023）**: ADR 0023は28行の数値`PaymentBand`境界（`lowerInclusiveYen`/`upperExclusiveYen`）を確定済みだが、claim-master schema v2（closed contract）には数値境界を運ぶmasterKindが存在せず、閾値をC#へ直書きすることはハード制約3で禁止されている。Task 13では正式式`AverageWageFormula`の算出額提示（`ComputedAverageWageMonthYen`）までを実装し、算出額→band導出と宣言済み`OfficeClaimProfile.AverageWageBandOption`との矛盾検知readinessは、PaymentBand境界マスタ（schema拡張＋seed）が入ってから実装する。それまで届出済みoptionが入力の正である。
- [ ] **[Phase3-1/Task13] R8-06改定対象（reform-target）の新12区分基本報酬行とoption 10のR8状態対応（2026-07-19 追加 / ADR 0023・0027）**: ADR 0027決定6はR6基本報酬行の改定対象外向け継続（135/135）だけを検証し、改定対象の新12区分（option 11〜22）のservice-code・単位数実値は値抽出ADR未確定のためseedへ投入していない。`ReformTarget`事業所の2026-06以降は、transition-rulesの状態別許可option集合と`ClaimPreviewPipeline`の経過措置guardでフェイルクローズし、無検証のR6単価請求を生成しない。また体制届option 10（生産活動支援）と`R8ReformStatus`の組合せは一次資料から確定できないため、R8-06版の状態別許可集合へ含めず算定不能とする（報酬体系IV〜VIは本スライスの請求対象外）。
- [ ] **[Phase3-1/Task11] 食事提供体制加算の附則経過措置期限（2026-07-19 追加 / ADR 0028）**: 同加算の期限を定める附則条文を登録済み一次資料から確認できない。登録版束（〜`claim-master-r8-06`）の範囲では現行本文に存在することを確認済みだが、期限を跨ぐ将来月の有効性は附則を登録・確定してから決める。
- [x] **事業所体制の加算フラグキー一覧（2026-07-11 クローズ / ADR 0021）**: [ADR 0021](decisions/0021-office-capability-official-codes.md) で公式体制項目と選択番号に基づくone-hotキー、適用版、暫定キーの明示的再登録、未知・旧・版外キーのフェイルクローズを確定した。`mealProvision` / `transportSupport` を請求コードへ推測変換しない。実マスタ解決と入力UIはPhase 3-1で実装する。
- [x] **国保連請求CSVの公式仕様（2026-07-11 クローズ / ADR 0024）**: [ADR 0024](decisions/0024-kokuhoren-csv-and-field-mapping.md) で共通編・事業所編の版、CP932、CRLF、外側レコード、項目順・必須条件・最大バイト数を確定した。`CsvSpecificationCompletenessTests` / `ClaimFieldMappingCompletenessTests` で仕様443 fieldIdsと3帳票113 fieldIdsの集合一致を検証する。CSVバイト生成はPhase 3-3で実装する。
- [ ] **[claim-master] R8.6 サービスコード表の実値投入（2026-07-20 追加 / 2026-07-25 記述訂正）**: **当初の記述（「一次資料 URL / SHA256 / 取得日 未確定」）は誤りだった。** `src/Tsumugi.Infrastructure/ClaimMasters/Seed/sources.json` に `r8-service-codes-1-pdf` / `r8-service-codes-1-xlsx` / `r8-service-codes-2-pdf` / `r8-service-codes-2-xlsx`（令和8年6月施行分、URL・SHA256 付き）が既に登録済みである。また CSV 生成はサービスコードを解決せず確定 snapshot の値を転記するだけなので、Phase 3-3 のブロッカーではない（[ADR 0031](decisions/0031-service-code-r8-06-service-code-table.md)）。**残る作業は登録済み出典から `service-codes.json` へ実値を投入すること**で、下記「R8-06改定対象（reform-target）の新12区分基本報酬行」と同じ作業単位。R6 分 151 entries と同様に source row manifest 付きで投入する別スライスとして扱う。
- [x] **[Phase3-3] `provider:*` 30 フィールドの正確な `ClaimInput` プロパティ集合（2026-07-20 追加 / 2026-07-25 クローズ）**: 実装時に `field-mapping-r7-10.json` の `status: "missing"` 30 件を列挙したところ、**対象プロパティ 30 件はすべて Phase 3-1 で Domain へ追加済み**であり、新規プロパティも migration も不要だった（対象は `Certificate` / `ClaimInput` / `ContractedProvider` / `DailyRecord` / `IntensiveSupportEpisode`）。plan Task 10 が代表例として挙げていた `ContractStartDate` / `ContractSupplyQuantity` / `ProviderRemarks` / `TransitionalMeasureCategory` / `ParentBurdenAmountYen` / `MunicipalReviewNote` は、実際の mapping には存在しない推定値だった。残っていたのは入力 UI と readiness 宣言であり、UI は例外利用日 4 項目（`ClaimInputView`）で閉じた（下記 Phase3-2/Task7 follow-up 項目を参照）。証跡: `docs/phase3-3-acceptance.md` §3・§7。
- [x] **[Phase3-3] 地域区分コードの公式値（2026-07-25 追加・同日クローズ / ADR 0033）**: 共通編 1.4 コード一覧（物理21頁）から確定した。`11:一級地`〜`17:七級地`、`23:その他`（01〜05 は特別区・特甲地・甲地・乙地・丙地）。**実装は誤って級地番号のゼロ詰め `01`〜`07` を出力していた**（`06` は公式コードに存在しない）。出典付き `region-classification-codes-r7-10.json` へ外部化し `RegionClassificationCodeCatalog` が解決する。`RegionGrade.None` は fail-close。証跡: `RegionClassificationCodeCatalogTests`。
- [x] **[Phase3-3] `provider:J121:02:008`（開始年月日）の意味（2026-07-25 追加・同日クローズ / ADR 0032・0033）**: 事業所編「開始年月日の設定方法」から確定した。就労継続支援B型（46）は「平成18年4月1日以降における最初にサービス提供した日」で、**契約支給量の変更があっても変わらず、当月より前の日付になりうる**（データ設定例: 10月の明細書に9月3日を記載）。ADR 0032 の個別入力方式が正しいことを確認し、誤っていた「契約日以降」検証を下限 2006-04-01 へ修正した（CSV 仕様の `lowerBound=20060401` と一致）。
- [x] **[Phase3-3] 訪問支援特別加算の算定回数・算定時間数と施設外支援の年度累計は個別入力が必要（2026-07-25 追加・同日 意味論確定・同日クローズ / ADR 0036）**: 登録済み一次資料から意味論を確定した。**(a) 訪問支援特別加算**（`provider:J611:01:051` サービス提供回数 / `:052` 算定回数 / `provider:J611:02:027` サービス提供時間数 / `:028` 算定時間数）— 留意事項通知 `r8-calculation-note`（SHA `0c4f357f…`）2(6)⑨ は「所要時間については、**実際に要した時間により算定されるのではなく**、計画に基づいて行われるべき指定サービス等に要する時間に基づき算定される」「1月に2回算定する場合は、加算算定後又は利用後、**再度5日間以上連続して利用がなかった場合にのみ**対象」と定める。**算定回数・算定時間数は日次実績（`DailyRecord.SpecialVisitSupportMinutes`）から導出できない。** **(b) 施設外支援**（`:053` 当月（日）/ `:054` 累計（日／180日））— 就労系留意事項通知 `r6-employment-guidance`（SHA `185fa992…`、物理16頁）1(1)① は「**毎年4月1日に始まり翌年3月31日をもって終わる1年間に180日間を限度**として算定する。この『180日間』とは利用者が**実際に利用した日数の合計数**」と定める。**直近180日のローリング窓ではなく年度累計**であり、当月分は日次記録から算出できるが年度累計は確定 snapshot（当月分のみ）から算出できない。なお同④アは「同日に施設外支援及び通常の施設利用を行った場合、施設外支援の実施日として扱う」。**残る作業（実装）**: `ClaimInput` へ月次2項目（施設外支援の年度累計日数・訪問支援特別加算の算定回数）、`DailyRecord` へ日次1項目（訪問支援特別加算の算定時間数）を追加し、ADR 0032 と同じ個別入力方式で供給する（migration×2・snapshot 拡張・mapping 4項目の `generated`→`missing` 変更・readiness・入力UI×2）。それまで該当加算を算定する事業所では CSV 生成が fail-close する。**クローズ（2026-07-25）**: 実装を完了した。`ClaimInput.SpecialVisitSupportBilledCount` / `ClaimInput.OffsiteSupportCumulativeDays`（月次）と `DailyRecord.SpecialVisitSupportBilledHours`（日次・単位は「時間」）を migration `Phase33GroupBExplicitAdditionInputs` で追加し、確定 snapshot・入力UI（`ClaimInputView` / `DailyRecordView`）まで配線した上で、mapping は 4項目のうち **3項目**を変更した — `:052`・`:054` を `missing`（個別入力）へ、`:028` の `targetProperty` を `SpecialVisitSupportBilledHours` へ。**`:051`（サービス提供回数）と `:053`（当月日数）は日次実績から導出できるため `generated` のまま維持**した（当初の「4項目を missing へ」は誤りだった）。あわせて `:027`（サービス提供時間数）は分をそのまま出していた単位誤りを、公式書式の 1/100 時間へ変換する `existing` へ改めた（丸め規則は下記の新項目で追跡）。readiness の実効性は `ClaimPreviewProductionWiringTests.Real_embedded_requirement_provider_requires_special_visit_billed_count_when_a_visit_day_exists` / `..._requires_cumulative_offsite_days_when_an_offsite_day_exists` で固定した。
- [ ] **[Phase3-3] 訪問支援特別加算「サービス提供時間数」（`provider:J611:02:027`）の分→1/100 時間変換の丸め規則（2026-07-25 追加）**: 事業所編「就労継続支援Ｂ型日ごと明細情報」項目27 は「実際にサービス提供した時間数（時間）を**整数部 2 桁・小数部 2 桁**で設定（例: 1.5 時間→0150、または 150）」と定め、桁数（numeric 4）と尺度（1/100 時間）は確定している。一方**本アプリは実績を分で持つ**（`DailyRecord.SpecialVisitSupportMinutes`）ため、分が 3 の倍数でないとき（例: 50 分 = 83.33…／100 時間）は小数部 2 桁へ丸める必要があるが、**丸め方向（切上げ／切捨て／四捨五入）も丸め桁も登録済み一次資料に規定が見つからない**。留意事項通知 `r8-calculation-note` 2(6)⑨ が「所要時間は実際に要した時間ではなく計画に基づいて算定される」と定めることから、実務上は計画所要時間（30 分単位等）が入り丸めが発生しない可能性が高いが、推測で既定の丸めを入れると加算の算定時間が根拠なく動くため、**割り切れない分値は fail-close する**方針を採った（`ClaimCsvModelPath.HundredthsOfHour` / `ClaimCsvGenerationReason.UnresolvableRule`。ADR 0036）。確認事項: (1) 項目27 は分単位の実績を丸めて設定するのか、それとも計画上の所要時間（時間単位）を設定するのか、(2) 丸めが必要な場合の公式規則。証跡: `ClaimCsvSpecialVisitAndOffsiteSupportTests.The_service_time_is_written_in_hundredths_of_an_hour` / `..._Minutes_without_an_exact_hundredths_of_an_hour_value_fail_closed`。
- [x] **[Phase3-3] CSV 引用規則における全角カナ・全角記号の扱い（2026-07-25 追加・同日クローズ / ADR 0033）**: 共通編 1.2.2(4)（物理6頁）から確定した。引用対象は「カンマ・ダブルコーテーション・スペース(0x20)・**漢字（2 バイトコード）**」で、**仕様は「漢字」を 2 バイトコードとして定義している**ため全角カナ・全角記号・全角数字も引用対象。**実装は表意文字だけを見ており全角カナ氏名を引用していなかった**（仕様違反）。あわせて同 (3)① の使用不可能文字（シングルコーテーション 0x27）の拒否を追加した。証跡: `CsvCellEncoderTests.EncodeCell_quotes_every_two_byte_value` / `..._fails_on_a_single_quotation_mark`。
- [x] **[Phase3-3] データ種別（`common:outer:control:005`）の混在ファイル解釈（2026-07-25 追加・同日クローズ / ADR 0033）**: 共通編 1.6 から確定した。「続いて格納するデータレコードの交換情報識別番号の上 3 桁を設定する。また、**同一ファイル上に複数の交換情報識別番号のデータレコードがある場合には、最初に格納するデータレコードの交換情報識別番号の上 3 桁**を設定する」。混在は仕様が想定しており、実装（先頭内側レコードの上3桁）は正しい。なお同節は「一部の交換情報については例外として直接対応しないデータ種別の設定が必要な場合がある」とし物理44頁に対応表を置くが、当該表はテキスト抽出できなかった。就労継続支援B型の請求が例外に該当するかは表の目視確認が必要（残課題）。
- [x] **[Phase3-3] golden CSV は公式サンプルとの突合ではない（2026-07-25 追加・同日クローズ / 突合不能と確定）**: 公式サンプルCSVおよび取込テストデータは**公開されていない**ことを確認した（`interface-index-r7-10` の index ページは施行分ごとのインタフェース仕様書のみを掲載し、サンプル・テストの語が存在しない。共通編・事業所編の「記載例」は帳票 PDF であり CSV ではない）。実データでの突合には公式の電子請求受付システムのテスト環境が必要で、これは電子証明書・伝送を伴うため **CLAUDE.md §責務境界の外**（アプリは生成までが責務）。したがって golden は「意図しないバイト変化の検出」に用途を限定し、**仕様適合は登録済み一次資料に対して固定する方針へ切り替えた**（[ADR 0033](decisions/0033-csv-official-spec-conformance-from-registered-sources.md)）。運用開始時は事業所側で公式システムへの取込テストを一度実施することを前提とする。
- [x] **[Phase3-3] 明細書「契約情報」レコードと開始年月日に必要な契約情報（2026-07-25 追加 / 同日クローズ・ADR 0032）**: `provider:J121:05`（契約支給量・契約開始/終了年月日・事業者記入欄番号）と `provider:J121:02:008`（開始年月日）を、日次記録からの導出ではなく**受給者証「サービス事業者記入欄」（`ContractedProvider`）の個別入力**を正本とする方式で解消した（[ADR 0032](decisions/0032-contract-information-as-individual-input.md)）。4 項目は既存の入力面（`CertificateView`）にあり、足りなかった初回サービス提供日を `ContractedProvider.FirstServiceDate` として追加した（migration `Phase33ContractedProviderFirstServiceDate`）。確定時に `ClaimFinalizationContractedProviderSnapshot` へ焼き込み、CSV はそれだけを読む。証跡: `ClaimCsvExportProductionWiringTests.Real_wiring_generates_cp932_csv_and_appends_the_export_history`（契約情報あり→生成成功）/ `..._fails_closed_when_the_finalized_snapshot_has_no_contract_information`（未入力→fail-close）。**残る注意**: Phase 3-3 より前に確定した分は snapshot に契約情報を持たないため、CSV 化には再確定が必要。
- [x] **[Phase3-2/3-3 共通] CSV・3帳票がいずれも未検証 raw aggregate から生成されていた（2026-07-25 追加・同日クローズ / ADR 0035）**: 検証実装を Application の `ClaimHistoryVerifier`（write 経路の `ClaimFinalizationStore` も委譲する唯一の実装）へ移設し、`VerifiedClaimBatchProvider` が返す `VerifiedClaimBatch`（private ctor ＋ internal factory）からのみ成果物を作る形にした。検証項目は履歴構造・確定操作ID一意性・header/detail の版と作成者一致・Σdetail＝header・snapshot envelope の codec 検証・確定操作 payload SHA-256 の再構築照合。あわせて**帳票側が Cancel を除外してから最大 revision を採っていた**（取消済み請求が復活する）spec 違反も修正し、CSV と同じ実効 revision 規則に統一した。証跡: `VerifiedClaimBatchProviderTests`（11件・mutation で RED を確認）/ `GenerateClaimReportsUseCaseTests.GenerateClaimInvoiceAsync_rejects_a_history_whose_persisted_snapshot_was_tampered_with`。
- [x] **[Phase3-3] `provider:J121:04:009`（サービス利用日数）を `BilledDays` で代替していた（2026-07-25 追加・同日クローズ / ADR 0034）**: 事業所編の項目説明「**本体報酬を算定しない日において、各種加算のみを算定した場合も 1 日とカウントする。上限額管理加算、欠席時対応加算等も 1 日とカウントする**」により、Codex 指摘（欠席時対応加算日が漏れる）が公式条文で確定した。あわせて `provider:J121:02:010`「利用日数」は事業所編③「※上限額管理加算などの加算（**欠席時対応加算は除く**）のみを利用した場合も１日とカウントする」であり、**2 項目は別定義**と判明した（前者は含む・後者は除く）。算定器が cap 後の欠席時対応加算算定日数を返すようにし、確定 snapshot へ `ServiceUsageDays`（＝本体報酬算定日数＋加算のみ算定日数）を焼き込み、`J121:04:009` はこれを、`J121:02:010` は従来の `BilledDays` を用いる。snapshot が持たない確定分（Phase 3-3 より前）は fail-close する。証跡: `ClaimCsvAmountAndCountTests.The_two_day_counts_follow_their_own_official_definitions` / `..._fails_closed`。**残る注意**: 上限額管理加算のみの日は本アプリが日付を持たないため 0 として扱っている（下記の新規項目）。
- [ ] **[Phase3-3] 上限額管理加算のみを算定した日のサービス利用日数への算入（2026-07-25 追加 / ADR 0034）**: 事業所編は「上限額管理加算…も 1 日とカウントする」と定めるが、本アプリの上限額管理加算は月次の請求入力であり**特定の日付を持たない**ため、サービス利用日数への算入を 0 としている。実務上は上限額管理加算をサービス提供日と別日に算定することは想定しにくいが、別日に算定する運用がありうるかを確認し、必要なら算定日の入力を追加する。
- [x] **[Phase3-3] 請求CSVの既定ファイル名の命名規則（2026-07-25 追加・同日クローズ / ADR 0033）**: 共通編 1.2.1（物理5頁）から確定した。CSV 形式のファイル名は「英字で始まる半角英数字 8 桁以内の任意の文字列に拡張子として `.CSV` を付加したもの」。**Codex の指摘どおり実装（`kokuho_{事業所番号}_{年月}_{hash8}.csv`）は不適合だった**。データ種別3桁＋処理対象年月下4桁＋内容SHA-256先頭1桁の8桁＋`.CSV` に変更し、生成側（`ClaimCsvGenerator`）が組み立てる。同 1.7.2 が同一月の同一ファイル名を避けるよう求めるため、内容が異なれば名前も異なる。
- [ ] **[Phase3-3] 属性区分に基づく文字種規則の強制（2026-07-25 追加 / 2026-07-25 更新 / ADR 0033・0037）**: 共通編 1.2.3③ は「英数属性は**半角の英小文字は使用できない**」「英数項目に漢字（2バイトコード）を混在させない」「漢字項目に半角の英字・数字・カナ文字を混在させない」と定める。**属性区分そのものは ADR 0037 の機械抽出で全 424 項目について取得済み**（`provider-r7-10-item-tables.json` の `officialAttribute`。数値 241 / コード値 144 / 英数 37 / 漢字 2）で、`ProviderItemTableCrossCheckTests` が `dataType` との整合を固定している。残る作業は (1) 属性区分を運用 spec（`provider-claim-r7-10.json`）の項目定義へ持たせる、(2) `CsvCellEncoder` が属性区分ごとの文字種規則を強制する、の 2 点。現状は数字のみ属性（numeric / date / yearMonth）の検証と制御文字・使用不可能文字の拒否までを実装。
- [x] **[出典管理] 同一 documentId が別ファイルを指していた（2026-07-25 追加・同日クローズ / ADR 0020 追記・ADR 0038）**: `r8-grant-decision-administration-202606` は CSV 仕様側が厚労省 PDF（`d6e16722…`、live URL は 404 のため historical 保持）、制度マスタ側が北九州市の再配布 PDF（`c5070de8…`）を指していた。**利用者判断により正本は厚労省発出 PDF に統一**し、制度マスタ側の url/sha256/publisher/retrievedAt を正本へ合わせた。再配布 PDF は「実際に page を読んだ・再取得できる複製」として `applicabilityNote` に URL と SHA-256 を残す。page 付けの同一性は推測せず、**正本について記録済みの physical pages 233〜235 のテキスト SHA-256 を再配布 PDF が再現すること**（2026-07-25 に再取得して SHA-256 一致を確認）と page 数 262 の一致で確認した。テキスト SHA-256 の算出法（`pdftotext -layout` 出力の末尾空白除去）はどこにも記録が無かったため、今回復元して ADR 0020 に記載した。除外リストは持たず、相違が再発したら `SpecEvidenceLedgerTests.Documents_registered_in_both_registries_agree` が落ちる。
- [x] **[Phase3-3] 版ごとの readiness と、新版での再出力可否（2026-07-25 追加・同日クローズ / ADR 0040）**: 新版で必須項目が増えた場合を 3 パターンに分けた（①項目が増えない ②増えたが snapshot が持っている ③snapshot に無い）。①②で不要な再確定を強いないため、**版が違っても解決した版で生成を試し、③のときだけ項目単位で fail-close**する方針にした。不足項目は最初の1件で打ち切らず `ExportClaimCsvUseCase.ValidateAsync` が全件返し、UI が一覧表示する。出力履歴には「使った版」と「確定時の版」の両方を記録する。あわせて `Current` の定義を「登録済みの最新版」から**「その時点で適用される版」**へ修正した（事前登録すると全件版不一致になる穴）。**残り**: 版ごとの requirement（`GetRequirements(version)`）と確定 snapshot からの readiness 写像は未実装。「出せるか」は生成の試行で判定できるため、確定前に将来版で検査したい場合にだけ必要（下記の新規項目）。
- [x] **[Phase3-3] 確定前に将来の施行分で検査する（2026-07-25 追加・同日クローズ / ADR 0041）**: 要件を版ごとに引く形（`GetRequirements(version)`）にし、確定 snapshot からも readiness の値を組めるようにした。値の組み立て（path キーを書く場所）と条件評価をそれぞれ 1 か所へ集約し、確定前（DB 由来）と確定後（snapshot 由来）は素の値レコードへ詰め替えて同じ関数を通す。確定前のプレビューでは**事前登録済みの将来版**でも評価し、現行版に無い不足だけを警告として表示する（`IsReady` は変えない）。**範囲**: snapshot からの評価は「項目が入っているか」だけを答え、evidence（受給者証の確認記録・上限額管理結果票）は再現しない（snapshot が運ばないため。既定値で埋めると偽の合格/不足になる）。将来版との差分は**両方向**（`BecomesRequired` / `BecomesOptional`）を示し、緩む方向でも確定は止め続ける（現行版の適用期間内に提出する可能性があるため。自動で緩めない）。証跡: `ClaimFinalizationReadinessContextBuilderTests`（6件）/ `Execute_warns_about_upcoming_versions_without_blocking_readiness` / `Execute_reports_requirements_that_the_upcoming_version_drops` / `Execute_reports_both_directions_of_an_upcoming_change` / `Execute_does_not_report_a_change_when_only_the_issue_code_differs`。
- [ ] **[Phase3-3] 令和7年10月より前の施行分の仕様データ（2026-07-25 追加 / ADR 0039）**: 版レジストリには r7-10 のみ登録。処理対象年月 2025-09 以前は fail-close する（`CsvSpecificationVersionUnavailable`）。過去分の再出力が必要になった時点で、当時の施行分を版として追記する（索引には平成23年6月施行分まで遡って掲載がある）。
- [ ] **[Phase3-3] 日次スコープの汎用 pass-through 入力（2026-07-25 追加 / ADR 0042）**: 算定に効かない転記専用の項目は、CSV 仕様に `storage: "generic"` と宣言するだけで「入力欄が出る・readiness で要求される・CSV へ出る」ようにした（月次スコープ＝事業所×受給者×サービス提供年月）。**日次スコープ**（日ごとの値。例: 訪問支援特別加算の算定時間数）は UI が日付ごとの表になり別設計が必要なため範囲外とし、日次の新規項目は当面型付き列で追加する。必要になった時点で別計画として設計する。なお既存の転記専用 11 項目は既に型付き列で実装済みのため移行しない（ADR 0042 の決定）。
- [ ] **[出典管理] CSV 仕様の判断のうち出典未付与のもの（2026-07-25 追加 / 2026-07-25 更新 / ADR 0038）**: 証跡台帳 `spec-evidence-r7-10.json` の `knownGaps` は現在 2 件。(1) 例外利用日4項目を `crossFieldGroup` で一括必須にした判断（Phase 3-2 / ADR 0030 由来）、(2) Phase 3-1 で status を `missing` とした項目群の「なぜ個別入力にしたか」の判断根拠。いずれも項目名・桁数・属性は ADR 0037 の突合済みで、欠けているのは**判断の根拠**。当初 3 件目だった「共通編の外側レコード19項目（機械抽出が未対応）」は共通編の項目表を機械抽出して突合対象に加えたためクローズした（`common-r7-10-item-tables.json` 19 項目 / `ItemTableCrossCheckTests`）。
- [x] **[Phase3-3] 共通編の外側レコード 19 項目も機械抽出の対象にした（2026-07-25 追加・同日クローズ / ADR 0037 追記）**: 共通編は 1 頁に 3 表（コントロール／データ／エンド）が並ぶため、spec の record に `sourceTableCaption` を持たせ、「見出しの直下のヘッダ行から次のヘッダ行の手前まで」を 1 表として読む形にした。突合対象は**全 443 項目**になり、`generatorRule` の source アンカーも**372 件すべて**が検証対象になった。共通編で 2 件の差分が判明し、いずれも理由つきで宣言した — (1) `common:outer:control:003` は公式表が全角ハイフンマイナス(U+FF0D)の「ボリュ－ム通番」で、spec は長音符へ正規化している、(2) `common:outer:data:003` は可変長ペイロードで公式表が属性・桁数を空欄にしており、spec の 822 は導出値（証跡台帳へ `derived-byte-length` として登録）。
- [ ] **[Phase3-3] `common:outer:control:006` 市町村番号の設定値（2026-07-25 追加 / ADR 0033）**: mapping は `const(value=0; reason=sender-is-provider)` を宣言するが、共通編 1.2.3③ はコード値属性の全桁 `0` を「未設定として取り扱う」とし、必須項目（`requiredWhen=always`）の未設定はエラーと定める。事業所請求におけるコントロールレコードの市町村番号の設定値を一次資料で確認する。
- [ ] **[Phase3-3] 共通編 1.6 データ種別対応表（物理44頁）の目視確認（2026-07-25 追加 / ADR 0033）**: 同節は「一部の交換情報については例外として最初のデータレコードの交換情報識別番号とは直接対応しないデータ種別の設定が必要」とし対応表を置くが、表がテキスト抽出できなかった（画像または複雑表）。就労継続支援B型の請求（J111＋J121＋J611 混在）が例外に該当するかを目視で確認する。
- [ ] SQLite 暗号化（SQLCipher等）の採否（ADR 0003 で暫定判断、運用要件で再確認）。
- [ ] **[Security] NuGet audit suppression GHSA-2m69-gcr7-jv3q**: `SQLitePCLRaw.lib.e_sqlite3` 2.1.11（EF Core SQLite 経由のトランジティブ依存）に HIGH 深刻度のアドバイザリが存在し、現時点で修正バージョンが upstream から提供されていない。`Directory.Build.props` に `<NuGetAuditSuppress Include="https://github.com/advisories/GHSA-2m69-gcr7-jv3q"/>` を設定して抑制している。許容根拠: 修正版なし／完全オフラインのデスクトップアプリで SQLite がネットワーク面に露出しない／唯一の動的値を埋め込む生SQL経路（`VACUUM INTO`）にシングルクォートエスケープを適用済み。解除条件: upstream がパッチ済みネイティブバンドルを公開したタイミングで抑制を除去し、バージョンを更新する。**セキュリティ再レビュー必須。**
- [x] **App 層のオフライン遵守確認（2026-06-26 クローズ）**: `tests/Tsumugi.Infrastructure.Tests/AppOfflineComplianceTests.cs` で `System.Reflection.Metadata` の `PEReader`/`MetadataReader` を用いて `Tsumugi.App.dll` の `TypeReference`/`MemberReference` を直接走査し、`System.Net.*` 等の禁止 API への直接参照を CI で機械判定する（AC0-5）。allowlist は最小・理由付きで `AppOfflineComplianceTests.Allowlist` に明示（既定は空）。歯のある検査であることを `HttpClient` 一時混入で確認済み。実行時ネットワーク監視はスコープ外（直接参照禁止＋オフライン端末運用で十分とする）。
- [ ] **アーキテクチャ/オフラインテストは直接参照のみを検査**: `GetReferencedAssemblies()` はトランジティブ参照をたどらない。オープンな問い: 将来的に依存グラフ全体を検査できるアーキテクチャテストツール（例: NetArchTest）を採用するかどうかを検討する。

## Phase 1 → Phase 2/3 引継ぎ

- [x] **DailyRecord 多重 New 重複の検知（2026-06-28 クローズ / ADR 0015）**: `DailyRecordConfiguration` に `(RecipientId, ServiceDate) WHERE Kind=1` の partial unique index を追加 (migration `20260628015004_DailyRecordDuplicateNewIndex`)。`DailyRecordDuplicateNewIndexTests` でレース条件下の二重 New 挿入が `DbUpdateException` で拒否されることを検証済。
- [ ] **AppendOnlyGuard と EF Core bulk operations**: `AppendOnlyGuard.Inspect` は ChangeTracker 経由の `Modified`/`Deleted` のみ検出。`ExecuteUpdateAsync`/`ExecuteDeleteAsync` は ChangeTracker を経由しないため検出できない。現在の Repository 実装に bulk 呼び出しはないが、将来追加する際は別途ガードが必要。`ArchitectureTests` で append-only 型に対する bulk 呼び出しを禁止する案あり。
- [x] **報酬・CSVハードコード機械判定（2026-07-11 クローズ / AC3-0-2）**: [`ClaimSpecificationBoundaryTests`](../tests/Tsumugi.Infrastructure.Tests/ClaimSpecificationBoundaryTests.cs) と `ExternalSpecificationLiteralGuard` が、出典付きマスタ値のDomain/Application直書き、CSV仕様値のCsvアセンブリ外直書き、配置境界違反、未知sourceをRoslyn token単位で拒否する。任意の大整数を一律禁止せず、実際の外部仕様値との一致を検査する歯ありテストを採用した。
- [ ] **Avalonia GUI 目視確認 (AC1-8 補完、Phase 3-1/3-2/3-3 に継続)**: Phase 1 では `AccessibilityDefaults` の値・適用・XAML 配線を全て CI テストで担保したが、実機起動でのフォント拡大追従、Reduce Motion の Transition 抑止、各 View のタブ順とフォーカス移動は手動 QA でしか確認できない。Phase 2 着手前の予定だったが未実施のまま Phase 3-1 / 3-2 まで到達し、両 Phase の受け入れ証跡にも「GUI 手動貫通確認 未実施」として記録済み。**Phase 3-3 実装完了（2026-07-25）後も未実施のまま。対象は `ClaimInputView` の「例外利用日」Expander と `ClaimPreparationView` の「国保連請求CSV出力」セクション（`ProcessingMonth` 入力・fail-close エラー表示）。macOS/Windows 双方で 1 回ずつ、プレビュー→確定→帳票保存×3→CSV 生成→保存 の end-to-end を目視で確認する（2026-07-25 更新）。**
- [x] **OfficeCapability の正式コード集合（2026-07-11 クローズ / ADR 0021）**: [ADR 0021](decisions/0021-office-capability-official-codes.md) に正式キー集合、請求・決定コードとの責務分離、適用期間、暫定キー移行とフェイルクローズ条件を記録した。実際の再登録UIと算定はPhase 3-1で実装する。
- [x] **`UpdateOffice` / `UpdateRecipient` の actor 監査ログ（2026-06-29 クローズ / Phase 2）**: `UpdateOfficeUseCase` / `UpdateRecipientUseCase` で `IAuditTrail.RecordAsync` を呼び `actor` / `AuditAction.Update` / `TimeProvider` / 要約文字列を `AuditLog` に追記する実装に置換。空 actor は `ArgumentException` で弾く。各 UseCase テストで監査行が追記されることを検証済。
- [x] **性別など利用者属性の拡張判断（2026-07-11 クローズ / ADR 0024・AC3-0-5）**: 令和7年10月事業所編のB型対象項目に性別はなく、全項目inventoryとmappingにも性別fieldIdはない。[ADR 0024](decisions/0024-kokuhoren-csv-and-field-mapping.md) に従い `Recipient.Gender` migrationは追加せず、`Certificate` の性別を請求へ推測流用しない。将来の公式版に必須fieldIdが追加された場合だけ、版付きmappingを更新して再判断する。
- [ ] **[Phase3-1/Task 9] 参加評価型（band-participation）と体制届option 10の対応**: ADR 0023はoption 10（`ProductionActivitySupport`）を「平均工賃`PaymentBand`の対象外」とのみ記録し、サービス費（Ⅳ）〜（Ⅵ）参加評価型行との対応を一次資料で確定していない。Task 9では基本報酬の8数値band条件だけをaverage-wage-band整数条件（公式option code equals 1〜7・9）へ再エンコードし、`band-participation`はpayment-band token条件のまま残してフェイルクローズ（builderの空PaymentBandに一致せず参加評価型行は解決不能）。対応が公式資料で確定するまで参加評価型の算定はスコープ外。
- [ ] **[Phase3-1/Task 9] 利用定員・人員配置区分の実データ源**: ADR 0021は基本報酬の`capacityClass`/`staffingClass`を`OfficeClaimProfile`の構造化入力と定めるが、現行エンティティ・migration・入力UIに未実装。`OfficeClaimBillingTokenProvider`は両者をnullで返し、`ClaimCalculationRequestBuilder`が`OfficeClaimProfile.CapacityHeadcount`/`OfficeClaimProfile.StaffingClass`のreadiness issueへフェイルクローズするため、profile拡張タスクが完了するまで本番のプレビュー算定は成立しない（use case・builderは注入値で全経路テスト済み）。
- [ ] **[Phase3-1/AC3-8] 51 missing fieldIds / 26 implementation targets（2026-07-20 一部クローズ / Phase 3-2 — 帳票21件のみ、Phase 3-3 spec/plan committed）**: 正本は [`docs/phase3-claim-field-mapping.md` の「Phase 3-1へ送る未実装入力」](phase3-claim-field-mapping.md#phase-3-1へ送る未実装入力-51-mapping-entries) とし、本ファイルへ51行を複製しない。CSV 30件＋帳票21件の51 missing fieldIdsは、`targetModel + targetProperty + uiSurface + migrationRequired`で束ねると26 implementation targetsであり、51実装項目ではない。2026-07-12時点でモデル、migration、repository、保存ユースケース、typed requirements、readiness gateは実装済み。**帳票21件（`report:*`）はPhase 3-2で実UI・`OperationLocalSnapshotReader`・snapshot v2経由の確定時凍結・3帳票抽出テキスト検証まで完了し、2026-07-20クローズ（証跡: `docs/phase3-2-acceptance.md` §1〜§2）。** 残るCSV 30件（`provider:*`）は実UI・CSV writerが未実装のため未完了のままとし、Phase 3-3スコープへ引き継ぐ。全history・全detailsを既知旧版codec込みで検証できるまで、CSV生成をfail closedとする。**Phase 3-3 spec/plan committed（2026-07-20、`dece4c6` / `e8d1498`）**: spec `docs/superpowers/specs/2026-07-20-phase3-3-kokuhoren-csv-design.md`、plan `docs/superpowers/plans/2026-07-20-phase3-3-kokuhoren-csv.md`（18タスク単一垂直スライス・TDD準拠）。Task 10-12でprovider:* 30 UI + readiness、Task 2/4-9でCSV writer（ハイブリッド方式: strong-typed `IProviderRecordBuilder<T>` × 9 + spec JSON駆動 `CsvCellEncoder`）、Task 13で `ExportClaimCsvUseCase`（確定済み `ClaimBatch` ガード + fail-close + `ClaimCsvExport` append-only履歴）、Task 16で自前golden CSV 3種（Normal/Correction/CJK）バイトスナップショット一致（AC3-7）到達を計画。実装は別session、`feature/phase3-3-kokuhoren-csv` 上で subagent-driven-development で実行予定。
- [x] **[Phase3-2/Task7 follow-up] ClaimInput 例外的利用4フィールドが孤立した自己参照readiness条件のまま（2026-07-20 追加 / 2026-07-25 クローズ）**: `field-mapping-r7-10.json` に**新キー `crossFieldGroup: "exceptional-usage"`** を追加し、`ClaimInputRequirementProvider` が同一 group の条件を Any-merge して各要件へ配るようにした。公式出典（`requiredWhenSource`）に紐づく `requiredWhen` の単項条件は書き換えていない（計画では両 JSON の条件を `any(...)` へ書き換える案だったが、一次資料の意味を上書きするため採らなかった）。`uiSurface` を `ClaimPreparationView` → `ClaimInputView` へ変更し、`ClaimInputView` の「例外利用日」Expander で 4 項目を編集できるようにした。証跡: `ExceptionalUsageCrossFieldTests`（5 テスト）/ `ViewInputWiringTests.ClaimInputView_exposes_only_owned_fields_histories_and_keyboard_commands`（Phase 3-2 の `NotContain` を `Contain` へ反転）/ `docs/phase3-3-acceptance.md` §7。

## Phase 1 受給者証 様式準拠（2026-06-28 追加）

- [ ] **自治体差異**: 受給者証の様式は MHLW 告示で共通項目が定義される一方、自治体ごとに独自の補足欄（例: 通所給食提供時間帯・上限管理事業所の電話番号など）がある。Tsumugi では「主要セクションのみ準拠」とし、自治体独自項目は当面 `SupplyNotes` / `Notes` の自由記述で受ける（ADR 0010）。利用自治体ごとに具体的な追加項目が判明したら個別に enum 化を検討する。
- [x] **食事提供体制加算 / 高額障害福祉サービス費等（2026-07-11 クローズ / ADR 0020〜0022）**: [ADR 0020](decisions/0020-claim-master-sources-and-versioning.md) と [ADR 0022](decisions/0022-burden-cap-master.md) で、令和6年4月から令和8年6月以降まで切れ目のない5 release source chainを確定し、[ADR 0021](decisions/0021-office-capability-official-codes.md) で体制・サービスコード境界を分離した。令和6年内を含むsource gapは0。Phase 3-0では出典・版・規則だけを確定しており、単位数・利用額の外部マスタ投入と実計算はPhase 3-1で実装する。
- [x] **負担区分の月額上限金額表（2026-07-11 クローズ / ADR 0020・0022）**: [ADR 0020](decisions/0020-claim-master-sources-and-versioning.md) と [ADR 0022](decisions/0022-burden-cap-master.md) で `r6-04` / `r6-06` / `r7-01` / `r7-09` / `r8-06` の5 releaseを連続させ、令和6年を含むsource gapを0にした。制度区分、受給者証記載上限、上限額管理結果の優先関係を維持し、入力未指定、証上限未入力、管理結果未入力を0円扱いせずフェイルクローズする。制度額をC#へ直書きせず、外部マスタ実値と算定はPhase 3-1で実装する。
- [ ] **計画相談支援事業者マスタ**: 現状は受給者証ごとに事業者名を自由記述。複数受給者で同じ事業者を参照することが多いため、Phase 3 で `ConsultationProvider` マスタを切り出して FK 参照に変更するか検討する。
- [ ] **ContractedProvider と Contract の整理**: Phase 1 既存の `Contract` は自社事業所の利用契約のみ表現する。新規 `ContractedProvider` は受給者証「サービス事業者記入欄」に書かれる**全契約事業所**（他事業所含む）を網羅する。重複格納を避けたい場合は、自社契約は `Contract` 側のみで管理し `ContractedProvider` には他事業所のみ書く運用も検討する。Phase 2 着手前に運用方針を確定する。

## Phase 1 障害者手帳・フェースシート（2026-06-28 追加）

- [ ] **療育手帳の等級表記**: 自治体ごとに「A1/A2/B1/B2」「1〜4度」「重度/中軽度」など表記が異なるため、`DisabilityCertificate.Grade` は文字列で受けている（ADR 0011）。利用自治体の表記体系が確定したら、自治体ごとの選択肢を `ComboBox` の候補として提供できるよう設定で外部化することを検討する。
- [ ] **精神障害者保健福祉手帳の更新通知**: 2 年ごとの更新が必要。`NextRenewalDate` を保持しているが、期限が近い手帳を一覧でアラート表示するビュー（受給者証期限アラートと同様）は未実装。Phase 2 で追加する。
- [ ] **フェースシートの自治体/事業所独自項目**: 連絡先・医療・受給状況・生活歴の主要グループは実装済みだが、事業所により「行動障害特性」「コミュニケーション手段」「移動手段」「日課」等の追加項目を求める場合がある。利用事業所の様式が判明したら個別項目として追加するか、`AssessmentSummary` の構造化を検討する。
- [ ] **フェースシート履歴の差分表示**: append-only により全バージョン保持されるが、UI からは現行版しか見えない。Phase 2 で「直前バージョンとの差分」「変更履歴一覧」を提供する案あり。
- [ ] **障害者手帳と受給者証の障害種別整合**: 受給者証側の `Disabilities`（身体/知的/精神/難病）チェックと、障害者手帳の `Type` の整合性は現状チェックしていない。例えば「身体手帳のみ所持しているのに受給者証で精神もチェック」というデータが入りうる。Phase 2 で警告（エラーではない）として通知する案を検討する。

## Phase 2 工賃計算（2026-06-28 追加）

- [x] **KouchinModule.bas v5 の実挙動突合（2026-07-05 クローズ / ADR 0012 v2）**: `.xlsm` 検査で方式 Hourly・端数 HalfUp（`ROUND(…,0)` = `BD5`）・`HourUnitMinutes = 15`（`AY9 = 1/96日`）・年度起点 4 月を確認。ADR 0012 を「確定」へ書き換え済み。実装補足として丸めスコープは per-rate 集計後 1 回（.xlsm の per-day BD5 との semantic drift はユーザ承認済み）。
- [x] **平均工賃月額の正式定義（2026-07-11 クローズ / ADR 0023）**: [ADR 0023](decisions/0023-average-wage-and-r8-transition.md) で年間工賃支払総額、年間延べ利用者数、年間開所日数を用いる正式式、対象期間、0除算、令和8区分・経過措置、Phase 2 `AverageWageMetric`との責務分離を確定した。請求用`Logic/Claim/AverageWageCalculator`と版付きマスタによる実計算はPhase 3-1で実装する。
- [x] **特別手当の性格（2026-07-05 クローズ / ADR 0018）**: `.xlsm` の G 列（特別手当）を確認。利用者×月の任意支給であり、`WageAdjustment` append-only エンティティで受ける設計に確定。作業手当・職能手当は `WageSettings` 拡張で表現、特別手当のみ `WageAdjustment` に分離。
- [x] **QuestPDF ライセンス（2026-07-05 クローズ / Phase 4 S1 / ADR 0013 v2）**: Community 継続を確定。一次情報確認 (docs/superpowers/specs/2026-07-05-phase4-s1-font-embed-and-questpdf-license-design.md §15) で就労B型事業所の想定規模との整合を確認。閾値超過時の Avalonia 印刷経路フォールバック計画は ADR 0013 に記載 (実装は別 ADR)。多施設運営法人の consolidated 年商が閾値近傍の場合は再判定すること。公営運営 / 上場企業も再判定対象 (ADR 0013 v2)。
- [ ] **工賃確定後の下層訂正方針**: 自動再計算しない（Correction で履歴に残す）方針を ADR 0012 に併記済。次月調整 or 再確定の手順は運用ガイドへ。
- [ ] **職能手当閾値の妥当性**: `SkillAllowanceTiers` の閾値（55h/70h は 2025 年時点の運用者設定値）。UI から編集可能とする形で対応済みだが、他事業所での標準的な設定値があるか確認する。
- [x] **PDF 帳票の日本語フォント埋込（2026-07-05 クローズ / Phase 4 S1）**: Noto Sans JP (SIL OFL 1.1) Regular / Bold を assets/fonts/ に追加し Reporting アセンブリの EmbeddedResource として同梱。QuestPdfLicenseConfigurator.Initialize() で FontManager.RegisterFont(Stream) 経由で登録し、WageStatementPdfGenerator の DefaultTextStyle で "Noto Sans JP" を全 Document に適用。WageStatementPdfGeneratorTests / WagePaymentListPdfGeneratorTests の CJK 3 系統 + Bold ASCII substring assertion を復活。NOTICE に OFL 帰属追記済 (ADR 0013 v2)。
- [ ] **[Phase 4 S1 final review 由来 / I-1] PDF テキスト抽出時の康熙部首 (Kangxi Radical) 誤置換**: Noto Sans JP を埋め込んだ生成 PDF において、QuestPDF/SkiaSharp が生成する `ToUnicode` CMap が「山」「田」「工」「支」「一」等、一部の漢字を康熙部首 (Kangxi Radicals, U+2F00-2FD5) の対応コードポイントとしてエンコードすることを確認した。**レンダリングされるグリフ自体は正しい**（画面表示・印刷は問題なし）が、`pdftotext` や PdfPig 等の PDF テキスト抽出ライブラリでこれらの文字を含む文字列（例: 利用者氏名「山田太郎」）を抽出すると、部首の文字（⼭⽥等）に置き換わって返る。エンドユーザーが生成済み工賃明細 PDF から利用者名をコピー＆ペーストする、または PDF ビューアの検索機能で氏名検索する場合に、当該漢字を含む氏名で不整合が生じうる。原因は QuestPDF/Skia 側のグリフ→Unicode 逆引きテーブル生成ロジックであり、Tsumugi 側のバグではない（pdftotext でも同一挙動を確認済み、PdfPig 固有ではない）。回避策未定（QuestPDF 側の Issue 報告 or 代替 PDF ライブラリへの切替が必要になる可能性）。AC4-1（日本語フォント埋込）自体は「グリフ描画」の観点で完全達成しているため本件で再オープンしない。テストでの回避策は `tests/Tsumugi.Infrastructure.Reporting.Tests/KangxiRadicalNormalizer.cs` の畳み込みテーブルを参照（テストの成否には影響しない、抽出後の後処理のみ）。関連: ADR 0013 影響節。

## Phase 2 Codex review 由来クローズ（2026-06-29 追加）

- [x] **WageFund 重複 New 検知（2026-06-29 クローズ / ADR 0017）**: `WageFundConfiguration` に `(OfficeId, MonthKey) WHERE Kind=1` の partial unique index を追加 (migration `20260628204038_WageFundDuplicateNewIndex`)。`WageFundDuplicateNewIndexTests` でレース条件下の二重 New 挿入が拒否され、`Correction` は許容されることを検証済。
- [x] **工賃計算 工賃基礎の Present 限定（2026-06-29 クローズ / AC2-5）**: `WageBasisExtractor` を recipient×date の実効 `Attendance=Present` セットでフィルタするよう修正。Absent / AbsenceSupport / DailyRecord なし / Cancellation 後の WorkRecord がいずれも除外されることを `WageBasisExtractorTests` で検証。
- [x] **按分 Σ=原資 不変条件のゼロ重み対応（2026-06-29 クローズ / ADR 0016 / AC2-4）**: `AllocationPolicy.Allocate` の `totalWeight=0 && totalYen>0` 分岐を `RemainderPolicy` 別に決定論で実装（`LargestRemainder` は throw、`ReserveToOffice` は全額留保）。
- [x] **工賃 VM の事業所選択 UI 配線（2026-06-29 クローズ）**: `WageFundSettingsViewModel` / `WageCalculationViewModel` / `WageStatementViewModel` の 3 工賃 VM に `OfficeCapabilityViewModel` パターンで `ListOfficesUseCase` 注入 + `SelectedOffice` バインド + View ComboBox を追加。実 UI から `OfficeId`（および `WageStatement` では `Office`）が設定可能に。
- [x] **PDF 保存ダイアログの UI 配線（2026-06-29 クローズ）**: `IFileSaveService` 抽象 + Avalonia `IStorageProvider` 実装 (`AvaloniaFileSaveService`) を導入し、`WageStatementViewModel` に `SaveSelectedStatementPdfCommand` / `SavePaymentListPdfCommand` を配線、View に保存ボタン 2 つを追加。AC2-7 が VM/View/Service の 3 層で疎通。
- [x] **PDF 出力の決定論化（2026-06-29 クローズ）**: `WageStatementPdfGenerator` の `DateTime.UtcNow` を `TimeProvider` 注入に置換。同一入力＋同一 TimeProvider で同一バイト列を返すことを `Statement_pdf_is_deterministic_for_same_inputs_and_same_timeprovider` で固定。
- [x] **Reporting アセンブリのオフライン直接参照スキャン対象化（2026-06-29 クローズ）**: `OfflineComplianceTests.Tsumugi_assemblies_do_not_reference_network_libraries` の `[Theory]` に `Tsumugi.Infrastructure.Reporting` を追加。
- [x] **Domain ≥95% カバレッジ到達 + CI 強制（2026-06-29 クローズ）**: `WageStatementPolicyTests` / `FaceSheetTests` / `WageStatement.NewRecord/Correction validation` / `YearMonth` boundaries 等を追加し Domain line coverage を 85.83% → **98.03%** に。`build/ci.sh` の Domain 閾値を 70 → **95** に昇格。
